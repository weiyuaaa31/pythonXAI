const CONFIG = {
    // 1. 調整球的速度與大小
    ballRadius: 7,       // 球的半徑 (越大越容易打中磚塊)
    ballSpeed: 4,        // 球移動的基礎速度 (數字越大球速越快，建議 3~8)

    // 2. 調整擋板的尺寸與移動速度
    paddleHeight: 12,    // 擋板高度
    paddleWidth: 90,     // 擋板寬度 (如果覺得難度高，可以改為 120 以上)
    paddleSpeed: 7,      // 鍵盤操控時擋板移動的流暢度

    // 3. 調整磚塊數量與佈局
    brickRowCount: 4,    // 磚塊的「列數」（上下幾層）
    brickColumnCount: 7, // 磚塊的「行數」（左右幾排）
    brickPadding: 10,    // 磚塊與磚塊之間的縫隙大小

    // 4. 調整遊戲生命值
    initialLives: 3,     // 初始接球失敗的生命數

    // 5. 自訂磚塊顏色與分數
    brickStyles: [
        { color: '#f43f5e', score: 40 }, // 最高層磚塊
        { color: '#fb923c', score: 30 },
        { color: '#facc15', score: 20 },
        { color: '#4ade80', score: 10 }  // 最底層磚塊
    ]
};